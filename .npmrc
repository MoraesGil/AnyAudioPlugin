# npm configuration for AnySound Plugin

# Use exact versions in package.json
save-exact=true

# Disable package-lock.json generation (we're using .gitignore)
package-lock=false

# Set log level
loglevel=warn

# Use production settings by default
production=false
